{
  "cellexia": {
    "reviews_block": {
      "name": "Cellexia Reviews",
      "settings": {
        "heading": {
          "label": "Heading",
          "info": "Shown at the top of the review widget. Translatable in Translate & Adapt."
        },
        "show_summary": {
          "label": "Show AI summary and topics"
        },
        "show_media_strip": {
          "label": "Show customer photo strip"
        },
        "show_form": {
          "label": "Allow writing reviews"
        },
        "per_page": {
          "label": "Reviews per page"
        },
        "accent_color": {
          "label": "Star color",
          "info": "Default matches the Amazon-style orange."
        }
      }
    },
    "star_rating_block": {
      "name": "Cellexia Star Badge",
      "settings": {
        "show_count": {
          "label": "Show ratings count"
        }
      }
    },
    "embed_block": {
      "name": "Cellexia Reviews",
      "settings": {
        "enable_product_widget": {
          "label": "Show the review widget on product pages",
          "info": "Mounts the full review widget automatically — no app block needed. Works on every theme."
        },
        "placement_selector": {
          "label": "Widget placement (CSS selector, optional)",
          "info": "Leave empty for automatic placement below the product information. Enter a CSS selector to mount the widget after a specific element instead."
        },
        "show_pdp_title_badge": {
          "label": "Show stars under the product title"
        },
        "pdp_badge_position": {
          "label": "Product-page stars position",
          "info": "Where the star rating and count appear on product pages.",
          "options__1": {
            "label": "Directly under the title"
          },
          "options__2": {
            "label": "Under the tagline"
          }
        },
        "pdp_badge_selector": {
          "label": "Stars placement (CSS selector, optional)",
          "info": "Advanced: the stars are inserted after the first element matching this selector, overriding the position choice."
        },
        "enable_badges": {
          "label": "Show star badges on product cards site-wide",
          "info": "Adds star ratings next to product names on the home page, collections, and search results for products with published reviews."
        },
        "badge_style": {
          "label": "Badge style",
          "options__1": {
            "label": "Stars and review count"
          },
          "options__2": {
            "label": "Stars only"
          }
        },
        "badge_selector": {
          "label": "Card title element (CSS selector, optional)",
          "info": "Advanced: only needed if badges don't find your theme's product card titles automatically."
        }
      }
    },
    "overall_block": {
      "name": "Cellexia Overall Reviews",
      "settings": {
        "heading": {
          "label": "Heading"
        },
        "max_reviews": {
          "label": "Number of reviews to show"
        },
        "layout": {
          "label": "Layout",
          "options__1": {
            "label": "Grid"
          },
          "options__2": {
            "label": "Carousel"
          }
        },
        "show_distribution": {
          "label": "Show rating distribution bars"
        },
        "show_product_links": {
          "label": "Link each review to its product"
        },
        "cta_label": {
          "label": "Button label (optional)"
        },
        "cta_url": {
          "label": "Button link"
        }
      }
    },
    "defaults": {
      "heading": "Customer reviews",
      "overall_heading": "What our customers say"
    }
  }
}
